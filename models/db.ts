import mysql from 'mysql';
export const NJ_DATA = {
  bolSize: 'BIGGEST IN THE WORLD',
};
export const dbConfig = {
  host: 'localhost',
  port: 3306,
  user: 'root',
  password: 'tictoccroc',
  database: 'routine_db',
};

// connection.connect();

// connection.query('SELECT * from daily', (error, rows, fields) => {
//   if (error) throw error;
//   console.log('dailyInfo: ', rows);
// });

// connection.end();
